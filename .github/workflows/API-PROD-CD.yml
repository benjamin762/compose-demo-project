name: API PROD CD asdfdhfgsdhgjryst

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  cd:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
    # ci.yml bygger och pushar redan både i main och dev

      - name: Deploy
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'asdfdhfgsdhgjryst'
          slot-name: 'production'
          images: ${{ secrets.ACR_URL_PROD }}/din-tjanst:${{ github.sha }}
          publish-profile: ${{ secrets.PROD_AZURE_WEBAPP_PUBLISH_PROFILE }}

          #asdf