name: CD

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  cd:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
    # ci.yml bygger och pushar redan både i main och dev

      - name: Deploy
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'webappname2317894'
          slot-name: 'production'
          images: ${{ secrets.ACR_URL_PROD }}/din-tjanst:$(git rev-parse --short HEAD)
          publish-profile: ${{ secrets.PROD_AZURE_WEBAPP_PUBLISH_PROFILE }}

          #asdf